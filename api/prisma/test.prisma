datasource db {
  provider = "postgresql"
  url      = "postgresql://user:pass@localhost:5432/db"
}

generator client {
  provider = "prisma-client-js"
}

model Test {
  id String @id @default(uuid())
}
